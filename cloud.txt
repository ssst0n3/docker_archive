#cloud-config
write_files:
- content: |
    #!/bin/bash
    apt clean
    sync
  path: /tmp/clean.sh
  permissions: '0755'
- content: |
    #!/bin/bash
    set -x
    mkdir -p /etc/apt/keyrings
    curl -fsSL https://download.opensuse.org/repositories/devel:kubic:libcontainers:unstable/xUbuntu_$(lsb_release -rs)/Release.key \
      | gpg --dearmor \
      | sudo tee /etc/apt/keyrings/devel_kubic_libcontainers_unstable.gpg > /dev/null
    echo \
      "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/devel_kubic_libcontainers_unstable.gpg]\
        https://download.opensuse.org/repositories/devel:kubic:libcontainers:unstable/xUbuntu_$(lsb_release -rs)/ /" \
      | sudo tee /etc/apt/sources.list.d/devel:kubic:libcontainers:unstable.list > /dev/null
    echo 'deb http://download.opensuse.org/repositories/home:/ssst0n3/xUbuntu_22.04/ /' | sudo tee /etc/apt/sources.list.d/home:ssst0n3.list
    curl -fsSL https://download.opensuse.org/repositories/home:ssst0n3/xUbuntu_22.04/Release.key | gpg --dearmor | sudo tee /etc/apt/trusted.gpg.d/home_ssst0n3.gpg > /dev/null
    apt update
    apt install -y podman=4:4.4.3-0ubuntu22.04+obs10.1
    sync
  path: /tmp/install-podman.sh
  permissions: '0755'
- content: |
    #!/bin/bash
    set -ex
    /tmp/install-podman.sh
    /tmp/clean.sh
    sync
    echo "init.sh complete"
  path: /tmp/init.sh
  permissions: '0755'
runcmd:
  - /tmp/init.sh
cloud_init_modules:
  - [ write-files, always ]
cloud_config_modules:
  - [ runcmd, always ]
  - [ set-passwords, always ]
cloud_final_modules:
  - [ scripts-user, always ]
  - [ final-message, always ]